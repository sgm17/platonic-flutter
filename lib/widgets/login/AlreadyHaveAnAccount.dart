import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:platonic/providers/auth_provider/auth_provider.dart';

/* Text account_login
    Autogenerated by FlutLab FTF Generator
  */
class AlreadyHaveAnAccount extends ConsumerWidget {
  const AlreadyHaveAnAccount({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return GestureDetector(
      onTap: () => ref.read(authProvider.notifier).state = false,
      child: Align(
        alignment: Alignment.center,
        child: RichText(
            overflow: TextOverflow.visible,
            textAlign: TextAlign.center,
            text: const TextSpan(
              style: TextStyle(
                height: 1.152999997138977,
                fontSize: 16.0,
                fontFamily: 'Gilroy',
                fontWeight: FontWeight.w300,
                color: Color.fromARGB(255, 255, 255, 255),

                /* letterSpacing: 0.0, */
              ),
              children: [
                TextSpan(
                  text: '''Already have an account ''',
                ),
                TextSpan(
                  text: '''Login''',
                  style: TextStyle(
                    color: Color.fromARGB(255, 63, 141, 253),

                    /* letterSpacing: null, */
                  ),
                )
              ],
            )),
      ),
    );
  }
}
