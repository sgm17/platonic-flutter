import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:platonic/providers/flat_provider/providers.dart';
import 'package:platonic/providers/user_provider/providers.dart';
import 'package:flutter/material.dart';

/* Text title
    Autogenerated by FlutLab FTF Generator
  */
class DetailAddTenantInput extends ConsumerStatefulWidget {
  final int flatId;
  final bool ownFlat;

  const DetailAddTenantInput(
      {super.key, required this.flatId, required this.ownFlat});

  @override
  DetailAddTenantInputState createState() => DetailAddTenantInputState();
}

class DetailAddTenantInputState extends ConsumerState<DetailAddTenantInput> {
  final controller = TextEditingController();

  @override
  Widget build(BuildContext context) {
    Future<void> toggleAddFlatTenant(String email) async {
      await ref.read(flatProvider.notifier).addOrRemoveTenantFromFlat(
          email: email, flatId: widget.flatId, isAdd: true);
      controller.clear();
    }

    return TextField(
      controller: controller,
      enabled: widget.ownFlat,
      onSubmitted: toggleAddFlatTenant,
      textInputAction: TextInputAction.send,
      keyboardType: TextInputType.emailAddress,
      maxLines: 1,
      style: const TextStyle(
          height: 1.1530000141688757,
          fontSize: 14.0,
          fontFamily: 'Gilroy',
          fontWeight: FontWeight.w300,
          color: Color.fromARGB(255, 255, 255, 255)),
      decoration: InputDecoration(
        hintText: '''hello@example.com''',
        hintStyle: TextStyle(color: Colors.grey[400]),
        counterText: "",
        errorStyle: const TextStyle(fontSize: 0.01),
        contentPadding: const EdgeInsets.symmetric(
          horizontal: 10.0,
        ),
        enabledBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(10.0),
            borderSide: BorderSide.none),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(10.0),
          borderSide: BorderSide.none,
        ),
        errorBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(10.0),
            borderSide: BorderSide.none),
        focusedErrorBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(10.0),
            borderSide: BorderSide.none),
      ),
    );
  }

  @override
  void dispose() {
    controller.dispose();
    super.dispose();
  }
}
