import 'package:flutter/material.dart';
import 'package:platonic/domains/flat_repository/src/models/models.dart';

import 'widgets.dart';

/* Group wifi
    Autogenerated by FlutLab FTF Generator
  */
class FeaturesListItem extends StatelessWidget {
  final FeatureModel featureModel;

  const FeaturesListItem({Key? key, required this.featureModel})
      : super(key: key);
  @override
  Widget build(BuildContext context) {
    if (featureModel.name.isEmpty) {
      return const SizedBox(
        height: 67.0,
      );
    }
    return Container(
      height: 67.0,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(0.0),
      ),
      child: Column(children: [
        SizedBox(
          width: 40.0,
          height: 40.0,
          child: FeaturesListItemContainer(
            child: featureModel.icon,
          ),
        ),
        const SizedBox(
          height: 4.0,
        ),
        SizedBox(
          height: 23.0,
          child: FeaturesListText(text: featureModel.name),
        )
      ]),
    );
  }
}
